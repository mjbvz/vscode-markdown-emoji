{
  "name": "markdown-emoji",
  "displayName": "markdown-emoji",
  "description": "Adds emoji support to VSCode's built-in markdown preview",
  "version": "0.0.1",
  "publisher": "bierner",
  "license": "MIT",
  "engines": {
    "vscode": "^1.8.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:_markdown.onActivateExtensions"
  ],
  "main": "./out/src/extension",
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "typescript": "^2.2.1",
    "vscode": "^1.0.0"
  },
  "dependencies": {
    "markdown-it-emoji": "^1.3.0"
  }
}